window.APP_CONFIG = {
  // Set mode to 'supabase' when you have pasted your real values below.
  // Leave as 'local' to test the UI with browser-only storage.
  mode: 'local',
  supabaseUrl: '',
  supabaseAnonKey: '',
};
